"use strict" // tell the dum compiler that it is a newer version of the js
// premetive data type
let number = 2;
let float  = 2.1;
let bollean = true;
let bigInt = BigInt(2123^33); // nothing just store the large value for stack market
let string = "mamoon khan";
let empty  = null;      // simply mean there is fucking nothing in the variable it also a object
let zero   = undefined; // mean we did't give a value until yat who know

console.table([typeof number,typeof float,typeof bollean,typeof bigInt,typeof string,typeof empty,typeof zero]);
// non premetive data type 
// object 